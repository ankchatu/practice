pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build from git'
            }
        }
        stage('test') {
            steps {
                echo 'test from git'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy from git'
            }
        }
    }
}
